[reticulum]
  enable_transport = Yes
  share_instance = Yes
  shared_instance_port = 37428
  instance_control_port = 37429

[logging]
  # Valid log levels are 0 through 7 with default 4
  loglevel = 4

[interfaces]

  [[TCP Server Interface Nexus.OrangePI]]
    type = TCPServerInterface
    interface_enabled = True
    outgoing = True
    listen_ip = 0.0.0.0
    # Don't change the server port because it is mapped into the container by docker-compose.yml
    listen_port = 4242

  [[TCP Client Interface - Nexus.Home]]
    type = TCPClientInterface
    interface_enabled = True
    outgoing = True
    target_host = nexus.deltamatrix.org
    target_port = 4251

  [[RNode LoRa Interface 169]]
    type = RNodeInterface
    interface_enabled = True
    outgoing = True
    port = /dev/ttyRNODE2
    frequency = 169450000
    bandwidth = 41700
    txpower = 14
    spreadingfactor = 8
    codingrate = 5
    id_callsign = DD8SB-2
    id_interval = 600
#    network_name = myTwinCluster
#    passphrase = myTwinCluster
    flow_control = False

  [[TCP KISS Interface #1]]
    type = TCPClientInterface
    interface_enabled = False
    kiss_framing = True
    target_host = 127.0.0.1
    target_port = 8001
    network_name = myRadioCluster
    passphrase = myRadioCluster
